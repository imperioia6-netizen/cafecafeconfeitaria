/**
 * Prompt base MÍNIMO. Todo conhecimento vem do Vault (knowledge_base).
 */
export function buildAtendenteBasePrompt(): string {
  return `Voce e a atendente do Cafe Cafe Confeitaria no WhatsApp. Todo seu conhecimento esta no VAULT. Siga ele.`;
}
